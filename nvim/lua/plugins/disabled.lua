
return {

  -- NOTE: Only one of the following UI plugins should be active at a time: `alpha-nvim`, `mini.starter`, or `dashboard.nvim` if u have imported "lazyvim.plugins.extras.ui"
  -- These plugins serve the same purpose, so the other two should be disabled to prevent conflicts or redundancy.

  { "goolord/alpha-nvim", enabled = false },
  { "echasnovski/mini.starter", enabled = false },


  { "folke/flash.nvim", enabled = false },
}
